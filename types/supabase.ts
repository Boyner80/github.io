/**
 * Placeholder until Phase 1 generates real types from the deployed schema:
 *
 *   npx supabase gen types typescript --project-id <id> > types/supabase.ts
 *
 * See docs/DATABASE_SCHEMA.md for the schema this will reflect once the
 * migrations in Phase 1 are written and applied.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
